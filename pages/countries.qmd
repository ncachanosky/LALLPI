---
title: "Countries"
---

Every country with a published LALLPI score, in alphabetical order. Click a
country for its full history, sub-index breakdown, and governing-party
record.

```{python}
#| echo: false
#| output: asis

import sys
sys.path.insert(0, ".")
import pandas as pd
import _site_helpers as sh
from IPython.display import HTML, display

df = pd.read_csv("data/2025/output/index_2025.csv")
display(HTML(sh.build_countries_directory_html(df)))
```
